import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const EnterPhoneNumber = () => {
  return (
    <View>
      <Text style={{color:'red'}}>EnterPhoneNumber</Text>
    </View>
  )
}

export default EnterPhoneNumber

const styles = StyleSheet.create({})